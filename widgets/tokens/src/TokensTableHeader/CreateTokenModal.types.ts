export interface ReceivedToken {
    id: string;
    description: string;
    // eslint-disable-next-line camelcase
    expiration_date: null;
    // eslint-disable-next-line camelcase
    last_used: null;
    username: string;
    value: string;
    role: string;
}
