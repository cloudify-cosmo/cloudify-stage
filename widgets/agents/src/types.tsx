export type Agents = {
    id: string;
    ip: string;
    deployment: string;
    node: string;
    system: string;
    version: string;
    // eslint-disable-next-line camelcase
    install_method: string;
};
