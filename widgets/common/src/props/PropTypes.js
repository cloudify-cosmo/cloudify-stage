import StringOrArray from './StringOrArray';
import Toolbox from './Toolbox';
import Widget from './Widget';

Stage.defineCommon({
    name: 'PropTypes',
    common: { StringOrArray, Toolbox, Widget }
});
