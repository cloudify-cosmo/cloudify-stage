machine:
  node:
    version: 4.4.5

dependencies:
  override:
    - npm install
    - npm install webpack -g
deployment:
  s3:
    branch: master
    commands:
      - gulp build
        pwd: semantic
      - webpack --config webpack.config-prod.js
