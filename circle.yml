machine:
  node:
    version: 4.4.5
dependencies:
  override:
    - npm install
    - npm install webpack -g
    - gulp build
deployment:
  s3:
    branch: master
    commands:
      - webpack --config webpack.config-prod.js
