machine:
  node:
    version: 8.9.1
test:
  override:
    - echo OK
dependencies:
  override:
    - npm install --only=dev
    - npm install esdoc -g
deployment:
  documentation:
    branch: /.*/
    commands:
      - npm run doc
      - npm run docdeploy