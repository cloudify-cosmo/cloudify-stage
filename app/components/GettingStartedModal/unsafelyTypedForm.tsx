import { UnsafelyTypedFormField } from '../basic';

// TODO(RD-1837): remove it after forms will be changed to tsx version
// eslint-disable-next-line import/prefer-default-export
export { UnsafelyTypedFormField };
