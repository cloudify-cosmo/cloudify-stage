tosca_definitions_version: cloudify_dsl_1_4

imports:
    - cloudify/types/types.yaml

labels:
    csys-obj-type:
        values:
            - environment

blueprint_labels:
    csys-obj-type:
        values:
            - environment

node_templates:
    EnvironmentNode:
        type: cloudify.nodes.Root
