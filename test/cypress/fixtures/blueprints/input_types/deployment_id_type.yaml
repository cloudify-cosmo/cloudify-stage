tosca_definitions_version: cloudify_dsl_1_3

inputs:

  deployment_id_all:
    type: deployment_id

  deployment_id_name_starts_with_b:
    type: deployment_id
    constraints:
      - name_pattern:
          starts_with: b
